- name: Install Mysql Client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: Load Schema
  ansible.builtin.shell: mysql --host mysql-dev.devops-tools.online -uroot -pRoboshop@1 </app/schema/"{{component}}".sql


